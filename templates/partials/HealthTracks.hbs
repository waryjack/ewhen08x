<div class="ewhen header-flexitem healthbar">
 
                <table style="background:none">
                {{log "Healthtrack data: " data}}
                {{log "Healthrack actor: " actor}}
                {{log "Healthtrack system: " system}}
                    <tr>
                        <td><a class="adj-resource res-track" data-resource-name="lifeblood">{{localize "EW.attribute.resource.lifeblood"}} ({{resources.lifeblood.max}})</a></td>
                        <td>
                            {{#times resources.lifeblood.lasting}}
                            <i class="fas fa-circle res-box"></i>
                            {{/times}}
                            {{#times resources.lifeblood.regular}}
                            <i class="fas fa-times-circle res-box"></i>
                            {{/times}}
                            {{#times resources.lifeblood.value}}
                            <i class="far fa-circle res-box"></i>
                            {{/times}}
                            {{#times resources.lifeblood.fatigue}}
                            <i style="transform:rotate(-45deg);" class="fas fa-minus-circle res-box"></i>
                            {{/times}}
                        </td>
                        <td class="res-track">{{#if (setting "useCritical")}}<a class="adj-resource res-track" data-resource-name="lifeblood"><i class="fas fa-forward"></i> {{localize "EW.attribute.resource.critical"}}</a>{{/if}}</td>
                        <td>
                            {{#if (setting "useCritical")}}
                            {{#times resources.lifeblood.critical}}
                            <i class="fas fa-times-circle res-box"></i>
                            {{/times}}
                            {{#times (minus 5 resources.lifeblood.critical)}}
                            <i class="far fa-circle res-box"></i>
                            {{/times}}
                            {{/if}}
                        </td>
                    </tr>
                    {{#if (setting "useResolve")}}
                    {{#unless (or isTough isRabble)}}
                    <tr>
                        <td>{{#if (setting "useResolve")}}{{#unless isRabble}}<a class="adj-resource res-track" data-resource-name="resolve">{{localize "EW.attribute.resource.resolve"}} ({{resources.resolve.max}})</a>{{/unless}}{{/if}}</td>
                        <td>{{#if (setting "useResolve")}}
                            {{#unless isRabble}}
                            {{#times resources.resolve.lasting}}
                            <i class="fas fa-circle res-box"></i>
                            {{/times}}
                            {{#times resources.resolve.regular}}
                            <i class="fas fa-times-circle res-box"></i>
                            {{/times}}
                            {{#times resources.resolve.value}}
                            <i class="far fa-circle res-box"></i>
                            {{/times}}
                            {{#times resources.resolve.fatigue}}
                            <i style="transform:rotate(-45deg);" class="fas fa-minus-circle res-box"></i>
                            {{/times}}
                            {{/unless}}
                            {{/if}}
                        </td>
                        <td class="res-track">{{#if (setting "useResolve")}}{{#if (setting "useCritical")}}<a class="adj-resource res-track" data-resource-name="resolve"><i class="fas fa-forward"></i> {{localize "EW.attribute.resource.critical"}}</a>{{/if}}{{/if}}</td>
                        <td>{{#if (setting "useCritical")}}{{#if (setting "useResolve")}}{{#times resources.resolve.critical}}
                            <i class="fas fa-times-circle res-box"></i>
                            {{/times}}
                            {{#times (minus 5 resources.resolve.critical)}}
                            <i class="far fa-circle res-box"></i>
                            {{/times}}
                            {{/if}}
                            {{/if}}
                        </td>
                        <td colspan='4'></td>
                    </tr>
                    {{/unless}}
                    {{/if}}
                    
                    </table>
                 
                
                </div>